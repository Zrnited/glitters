export default function Loading (){
    return (
        <section className="px-5 flex justify-center items-center h-screen">
            <h1 className="italic font-semibold">Loading contents...</h1>
        </section>
    )
}